#stacks is data structure the operates on the top element
stack=list()
#we use push/append to add an element at the top
stack.append(5)
#we use pop to remove the element at the top
stack.pop()
print(stack)
#we use peek to retrive the value of the top element without removing it 